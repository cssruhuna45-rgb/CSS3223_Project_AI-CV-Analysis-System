fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0
langchain>=0.1.0
langchain-community>=0.0.20
langchain-core>=0.1.30
langchain-text-splitters>=0.0.1
langchain-google-genai>=0.0.10
google-generativeai>=0.4.0
chromadb>=0.4.20
pypdf>=4.0.0
python-dotenv>=1.0.0

# Local embeddings. app/rag/embeddings.py imports HuggingFaceEmbeddings
# from langchain-huggingface, which needs sentence-transformers to load
# all-MiniLM-L6-v2.
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# Token-aware chunking in app/rag/document_loader.py sizes chunks with
# the embedding model's own tokenizer, which comes from transformers.
transformers>=4.40.0

# Fallback tokenizer for the same chunking, used only when transformers
# cannot be loaded. Still needed: that code path exists.
tiktoken>=0.7.0
